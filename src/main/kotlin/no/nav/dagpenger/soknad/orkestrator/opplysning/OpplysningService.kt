package no.nav.dagpenger.soknad.orkestrator.opplysning

class OpplysningService {
    fun hentOpplysning(
        ident: String,
        søknadId: String,
        behandlingId: String,
        beskrivendeId: String,
    ) {
    }
}
